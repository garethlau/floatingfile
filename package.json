{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0",
  "name": "floatingfile-mono",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "dev": "yarn run dev:app",
    "dev:app": "lerna run --parallel --stream dev --scope={@floatingfile/client,@floatingfile/server}",
    "dev:landing": "lerna run --parallel --stream dev --scope=@floatingfile/landing",
    "build": "yarn run build:app",
    "build:app": "lerna run build --parallel --stream --scope={@floatingfile/client,@floatingfile/server}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "dependencies": {}
}
